package ai;

import main.Move;

public class MoveValue {
    Move move;
    int value;

    MoveValue (Move move, int value) {
        this.move = move;
        this.value = value;
    }
}
